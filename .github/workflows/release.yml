name: CI/CD Pipeline

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  packages: write

jobs:
  test-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25"

      - name: Setup Infrastructure (MinIO & Postgres)
        run: |
          make docker-up
          sleep 10 # Allow time for databases to initialize

      - name: Build and Start Application
        run: |
          go install github.com/swaggo/swag/cmd/swag@latest
          make swagger
          make build
          # Start app in background
          ./bin/media-service &
          sleep 5 # Wait for app to be ready

      - name: Run E2E Tests
        run: |
          make test

      - name: Build Production Linux Binary
        run: |
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -a -installsuffix cgo -o bin/media-service-linux-amd64 cmd/media-service/main.go

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=semver,pattern={{version}}
            type=raw,value=latest

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
